# Feature 0038: Remove My Orders panel from Cash Market Pro

## Summary

Remove the "My Orders" panel (Open/History tabs, order table) from the Cash Market Pro page layout. The target div is at position `top=605px, left=16px, width=391px, height=308px`, rendered within `CashMarketProPage` as the first column (`col-span-3`) of Row 2.

## Technical Details

- **DOM Path**: `div.grid grid-cols-12 gap-2[1] > div.col-span-3 min-h-0 flex flex-col[0]`
- **React Component**: `CashMarketProPage`
- **HTML Element**: `<div class="col-span-3 min-h-0 flex flex-col">` containing `MyOrders` component

## Files to Change

### `frontend/src/pages/CashMarketProPage.tsx`

1. **Remove the My Orders column** (lines ~491–499): Delete the entire `div.col-span-3` that wraps `<MyOrders ... />`.

2. **Adjust Row 2 layout**: Row 2 currently has 4 columns (My Orders, Activity, News, Impact) each `col-span-3`. After removal:
   - Remove the My Orders column
   - Redistribute the remaining 3 columns: Activity, News, Impact — each can stay `col-span-4` (or keep `col-span-3` for a narrower layout; the grid will naturally flow with 9/12 used). For visual balance, use `col-span-4` for each remaining panel to fill the 12-column grid.

3. **Clean up unused code**:
   - Remove `MyOrders` import (line ~23)
   - Remove `myOrders` from `useCashMarket` destructuring if no longer needed
   - Remove `handleCancelOrder` and `cancellingOrder` state if they are only used by `MyOrders`
   - Remove `cashMarketApi.cancelOrder` usage if no other component uses it on this page

4. **Skeleton loading**: Update the skeleton grid in Row 2 to match the new 3-column layout (remove one skeleton cell).
